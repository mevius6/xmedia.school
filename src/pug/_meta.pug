-
  let canonicalURL = (page.title[0] != '')
    ? data.meta.domain + page.path[1]
    : data.meta.domain
-
  let promoImage = `${data.meta.domain}/ogp_${(page.title[0] != '')
    ? page.id
    : 'common'
  }.jpg`
-
  let titleMerge = (page.title[0] != '')
    ? `${page.title[0]} │ ${data.meta.name}`
    : data.meta.name
-
  let keywordsMerge = (page.keywords != '')
    ? `${page.keywords},${data.meta.keywords}`
    : data.meta.keywords
-
  let titleSEO = (page.title[1] != '')
    ? page.title[1]
    : titleMerge
-
  let themeColor = {
    'dark': '#15141b',
    'light': '#edecee'
  }

meta(charset="utf-8")
meta(name="viewport"
  content="width=device-width, initial-scale=1, viewport-fit=cover")

meta(http-equiv="X-UA-Compatible" content="IE=edge")

title= titleSEO

meta(name="description" content= page.description)
meta(name="keywords" content= keywordsMerge)

//- meta(name="yandex-verification" content= YA_KEY)
//- meta(name="google-site-verification" content= GS_KEY)

//- https://web.dev/referrer-best-practices/
//- https://w3c.github.io/webappsec-referrer-policy/
//- meta(name="referrer" content="strict-origin-when-cross-origin")

meta(name="apple-mobile-web-app-capable" content="yes")
meta(name="apple-mobile-web-app-status-bar-style" content="black-translucent")

meta(property="og:locale" content="ru")
meta(property="og:title" content= titleMerge)
meta(property="og:site_name" content= data.meta.name)
meta(property="og:type" content="website")
meta(property="og:image" content= promoImage)
meta(property="og:image:secure_url" content= promoImage)
meta(property="og:image:type" content="image/jpeg")
meta(property="og:image:width" content="1200")
meta(property="og:image:height" content="630")
//- meta(property="og:image:width" content="1280")
//- meta(property="og:image:height" content="640")
meta(property="og:description" content= page.description)
meta(property="og:url" content= canonicalURL)

meta(name="twitter:card" content="summary")
meta(name="twitter:title" content= titleMerge)
meta(name="twitter:description" content= page.description)
meta(name="twitter:image" content= promoImage)

link(rel="apple-touch-icon" sizes="180x180" href="./icons/apple-touch-icon.png")
link(rel="icon" type="image/png" sizes="32x32" href="./icons/favicon-32x32.png")
link(rel="icon" type="image/png" sizes="16x16" href="./icons/favicon-16x16.png")
link(rel="icon" type="image/svg+xml" href="./icons/favicon.svg")
link(rel="mask-icon" href="./icons/favicon.svg" color= themeColor.light)
link(rel="alternate icon" href="./icons/favicon.ico")
link(rel="manifest" href="./site.webmanifest")
meta(name="msapplication-TileColor" content= themeColor.dark)

meta(
  name="theme-color"
  content= themeColor.light
  media="(prefers-color-scheme: light)"
)
meta(
  name="theme-color"
  content= themeColor.dark
  media="(prefers-color-scheme: dark)"
)
meta(name="color-scheme" content="dark light")
